// Public API address only. Never put a Notion token in a browser file.
window.PORTFOLIO_CONFIG = Object.freeze({
  apiUrl: "https://portfolio-notion-api.chocosdj.workers.dev/api/portfolio",
  refreshIntervalMs: 60000,
});
